fix(tests): mock Prisma database queries in emailVerification tests for CI runner compatibility

- Refactor emailVerification.test.ts to mock Prisma user queries with vi.spyOn
- Prevent database connection failure (ECONNREFUSED) when running tests in headless CI environments without PostgreSQL
