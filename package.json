{
  "name": "boardgametime-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/games/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:services": "docker-compose up -d",
    "dev:db": "dotenv -e .env -- pnpm --filter @boardgametime/db exec prisma db push && dotenv -e .env -- pnpm --filter @boardgametime/db seed",
    "dev:all": "pnpm dev:services && pnpm dev:db && pnpm dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "prettier": "^3.2.5",
    "turbo": "^1.13.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
